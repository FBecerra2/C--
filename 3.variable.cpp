#include <iostream>

using namespace std;

int main()
{
    int num1, num2, sum = 0;
    cout << "Enter any two numbers to find the sum:" << endl;
    cin >> num1 >> num2;

    sum = num1 + num2;
    cout << "The result of sum is : " << sum << endl;
    system("pause");
    return 0;
};